# Database (SQLite). Path is relative to apps/api.
DATABASE_URL="file:./dev.db"

# Optional: for production or custom port
# PORT=3001

# Optional: JWT secret (change in production)
# JWT_SECRET=your-secret-here

# Optional: when using ngrok so signing links work when sent to drivers
# PUBLIC_URL=https://your-ngrok-url.ngrok-free.dev
